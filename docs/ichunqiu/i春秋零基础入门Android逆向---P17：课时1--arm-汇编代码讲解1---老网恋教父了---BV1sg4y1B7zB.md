# i春秋零基础入门Android逆向 - P17：课时1  arm 汇编代码讲解1 - 老网恋教父了 - BV1sg4y1B7zB

🎼。

![](img/29c69f2d999fbe77a47d9d32620b6b39_1.png)

🎼嗯ん。嗯，好啊好的嗯，大家好，我是讲MF8。那么今天就来讲第十六课，从第十六课开始加是1个AI代码汇编的一个讲解。嗯，大概是有近五节课的内容来讲一下关于AI代码反面义，还有分析上的一些小技巧。

还有方法之类的。呃，来请大家注意一下这样一个安卓平台上的1个AIM的一个逆向分析呢，实际上是一个windows平台上的1个OD反回意义调试的时候是非常的类似的。

所以是如果有一个windows上的一个呃逆向经验的话，那么对于要学习这样一个起案的话，应该是比较简单的。那当然如果没有的话也不要紧了哦，那么就跟着我一起来学习。首先得跟得跟大家先来介绍一个工具。

就是这样1个IDA嗯，IDA是长这样一个样子的，它是我们呃在1个LM1个SO调试还尔科技上的最主要的一款软件。啊，可以看一下它的功能非常差强大，嗯，也支持一个插件扩展，还有这一个脚本扩展。

喜欢的话也可以为它来写一个调试器模块，或者说是一个呃分析模块之类的。嗯，这是他呃非常强大的一个地方。好，可以看一下。嗯。好，但是的话它在一个调试方面的一个功能的话是呃挺挺弱的啊，是挺弱的。

那不过也只能够用它目前来说啊呃带有图形。带有图形界面的一个调试器的话，就它就这样1个IDA中是比较好用的一款啊。嗯那当然如果不是那喜欢那种呃习惯了另一还有那种的话会有其他的选择。好。

那么但是也就为了一个呃解决的问题的话，我就这里就介绍一款IDA。好，那么接着我们就来说一下我们今天今天的一个目标吧。啊今天我们的话主要是用来对1个SO文件来进行一个调试。

然后来看一下它的1个AM代码的一个呃属性一个功能。好，那么接来说下1个AIM代码，1个SO文件。好，我们现在把我们的这样一个目标文件来打开。好，这一期我们的目标是在一个APK文件。

也是我们上一节课所编译所生成的1个APK文件。啊，当然里面的话我就做了一点小小的更改。哎，可以来看一下，我更改内容的话，就主要就是在于呃把一个let的一个位置给改了一下。

就是把一个let函数给做一点简单的一个修改。好，我们现在呃打开它。我们来哎在这里按按按卓去了已经法入了，我们切换到一个工程管理器的一个目录指向。好。来看一下这里有这么多文件夹，对不对？

这里有1个LRB文件夹，哎里面这里全放呢就是我们所谓的一个的一个动态库了。我们打开它可以发现它里面有非常多的一个指向。哎，这什么意思啊？这是根据我们的一个呃运行的一个机器的一个一个一个代码的一个不一样。

它位数不一样来选择一个不一同的一个SO文件呢进行一个加载。比如说你是一个AIM的一个系统，就可以选择上面的这三个好，这个有机器设定4位的，哎，就会选择这样一个文件夹里面的一个内容。

如果不是就会选择下面两个文件夹内容。好，这里后面的呢有一个大V切什么意思呢？这是我们一个AIM的一个版本的一个不一样。如如果是新网的一个手机。比如说哎比如说我身是的话。

它就它就是支持这一个V切的一个编码格式，它就会加载这样一个SO文件。如果是比较老的机器的话，就会再加载上面的这样。一个SO文件。好，那么它下面的话就是对应着miss，还有一个intel那个叉86。

还有叉64的一个配边。好，那么下面的话就不讲了。我们这里唉我们这里你我们这里因为我的手机是定了4好，它就支持这样1个I32位的个UC，所以它就会加载这样1个SO文件。

那么这里我们要分析的就是这样1个SO文件了。好，我们来再来找一下。看一下我怎么更改。好，这里我们来看到我的有一个一个lety函数。一个训章的一个类存函数。好，这就是我们的上一期课主要的一个内容。

我们就来登析一下，哎这我们加载起来的这样一个函数是怎么一个样子的。好。我们来打开这样一个SO文件，找到它然后对它进行一个加载分析。好，啊，我们就从这样1个APK中把它给抽出来。

我们先用一个现在跟大家说一下，所有的APK都是一个以ZIP压缩的格式，用1个二进16进制文件查看起来打开就知道了。它前缀是1个PKPK0304，这是一个非常明前的1个ZIP压缩包的一个标志位，一个魔术。

哎，所以看到这样一个的话就知道它是一个EZIP了。好，既然有不此，我们就用一个压缩软件来打开，我们用。好，找到1个ARP目目录下，然后自己的手所对应的1个SO文件，把它给抽出来。好。

那么这个软件就就可以让我们来直接的来进行一个分析了。好，分析也很简单，把目标文件给拖到IDK里面。那么它就会自动的完成一个对格式的一个分析，然后为我们选择上一个合适的一个加价器。才。

那么这里看到1个ELFOLM，这就是1个IDA的一个默认的一个加价器了。我们这啊下边的其他选项都按照默认默认来说就好了。如果不懂的话，就一先按照默认来说。好，那么我们按连接OK。

然后来让它来完成一个自动的一个分析。那么这样一来，哎他整一个分析就已经完成了。可以看一下，看一下他他的一些代码，他他一些代码。好嗯。嗯，那么这里的话我也们也可以去看一下发的一些实现代码。

比如说找到这样一个JR no函数，也就是一个呃我们找到它hello三函数吧，也就是我们摄一杰克所写的一个本地函数，我们那时候写的是CCC加加代码。哎。

那么双击双击就能够跳转到hello斯3的一个函数里面了。我们这里就可以看到它的一个实现内容。唉，它是以这一种形式来实现我们的C加加的一个内容了。实际上这一些的话就是我们所所叫的1个AI会变代码了。

这里每一条都是一条AIM语句，然后这么多条AI语句来组合，就来就能够实现我们想要的一个把它一个字符串的一个功能了。嗯，这就是1个AI代码。这实际上与那个呃smarly代码也是一样的。

也是一样由多条smar语句来组成一个函数的。这里的话就只是把一个smary给换成了1个AI语句而已。那么这一这里每一条AI语句都会对应着一个不一样的一个功能。那么到底它的功能是什么呢？哦。

唉我们我们到后面再来慢慢的确认，我们来对它进行一个调试来看一下它的一个效果。好，调试的时候，现在它对应的位置下断点。好，这里我要来测一下hello3的一个功能。我们这里下好一个断点。啊。

那这里断点机下好了，我们就来开始进行一个测试吧。呃，调试的时候的话哎，请大家注意调试的话，我们就需要先来进行一些初始的一些操作，才能够正才能够正常的来把这样一个软件给调试起来了。

我们先来呃对现在连接上手机，然后把一个目标文件给安装上。靠。哦我们要下安phone这样1个AP文件。嗯。



![](img/29c69f2d999fbe77a47d9d32620b6b39_3.png)

接着我们就来呃进行一个调试一些前前期操作啊。首先我们调在调试的时候，需要先在那个手机中来启动一个服务服务端口，然后再在这样一个服务端口中转到出我们需要数据到IDA之中的。好。

我们现在找到这样一个服务端口。它是在IDA目录下的这样一个debuox server中。等一个目录下的。好，我们找到IDA目录。里面可以找到一个deb server的文件夹，双击进去，哎。

里面就有一个安卓serv了。那么这样一个安卓serv的话就是我们的一个目标文件，那么就能够这个文件的话就是一个呃我们的目标。我们把这样一个push到手机上面去，然后给赋予一个启动权限。然。

然后对它进行一个启动。把它这推送到手机上去的话，是使用APP1个push的指令。呃，前面应该说过了，好第一个参数是一个呃文件呢，在一个本机录出的位置。然后第二个参数的话就是在目标的一个位置。

这里我们就push到1个TIP文件夹入中。如果没有的话，你可以自己去选那一个合色文件夹。好，然后我们使用APP效以后进入到它的一个呃机器自动，然后使用SU来进入到来获取一个权限。

然后我们再来进入到我们的1个TIP目录，也就是刚才push的一目录之下呢，来这里我们来看一下。靠。嗯嗯嗯嗯。好，刚才我们 pushush过来的是一个呃安卓斯，我们再来 pushush一遍。好。

那这一次我们push过去的话，就对它进行一个重命名。我们重命名1个ANDserv呃，就千万不要用这样一个安卓serv的一个名字啊。因为现在有一些可的话是会检测到这样你这样一个名字命名的程序的。

如果检测得到就不会启动了，我们就对push进去的文件进行一个重命名。这里我push过去的话，就重命名为这样一个名字。好，我们到这一个文件夹中。



![](img/29c69f2d999fbe77a47d9d32620b6b39_5.png)

唉，可以看到了可以看到了。好，这里它目前来说有两个文件，一个是我刚才第一次复ush过去的安卓生我。然后第二次是我刚复ush过去的ANDSE呀。好，那么我们就对这样1个ANDSER来赋予一个执金权限。

然后对它进行一个运行。啊，我们用CHMD的指令来给它执行一个权限。我们给他权限给他的一个权限是Z77。好，我们给他一个信新权限以后呢，这样1个ANDserv的话已经能够正常的运行了。

我们我们给对它进行一个执行。对，可以看到它运行起来以后呢，呃就会有这样一行提示，就是1个IDA的一个远程调试器已经执行了。然后它就会这样一个端口来启动一个监听。好，这里我的我端口是23333。

那么里面的端口可能有点不一样。如果想要改变端口的话，也可以使用一个杠P指令来对它进行一个修改。使用一个杠P，然后一个后面ge的目标端口。947。比如说这样哎。

那么这里就可以切换到23947的一个端口来进行一个调试了。来，有一些呃有一些可，比如说360，比如说360就可以默默默认的去监控它的一个原始调试端口。如果被检测到的话。

可以尝试的去换另外一个端口来进行一个调试了。好，我们这里我就使用一个按按卓设的一个默端口。好，看到它哎，记录记住这样一个端口，它目前是在这样1个23333端口来进行一个接听的。

接着我们就需要对端口进行一个转发了。好，看一下看一下。

![](img/29c69f2d999fbe77a47d9d32620b6b39_7.png)

这里我们的一个手机上端口是2333，我们来进行一个端口转发，重新启动另外1个CMD指令，然后进用使用这样1个AP four指令。前面已经看过了AP four。



![](img/29c69f2d999fbe77a47d9d32620b6b39_9.png)

PCP首先第一个是PC上的端口。然后第二个它是手机上的端口。我们进行一个转发。那么这个时候手机上2333端口和PC上的这样一个端口就建立了一个链接了。接着我们再对1个IDA的一个调试。

我们来选择对应的一个端口，在一个debugger一个process option里面。哎，这里一个这里填一个loc host，也就是17。0。0。1，这里也可以填一个手机的1个ID地址也是可以的。

然后一个转换的一个端口和我们刚才转换的一个位置是一样的，也就是这样一个端口。好，我们填2333。确定。啊。那么这个时候的调试一个设置已经设置好了。那么这一个时候呃为了能够让程序给调试起来。

我们还需要手动的去启动一个程序。启动程序的时候哎还是用这一条指令，这一条指令在前面的已经接束过了。这一条指令在运行以后呢，就会让程序以其以调试的方式给启动起来。

并且停留在一个啊还没有加载程序的一个呃一个位置。好。年大。有一个杠G杠N指令package栏，还有一个process。我们来看一下哈，这个酒量是什。



![](img/29c69f2d999fbe77a47d9d32620b6b39_11.png)

咱门嗯继续找到啊，是这样一个。嗯。然后第二个是。好，然后以我靠找不到。スセス。ほ了这个面不ちち。我们来进行一个提动。好，那这样也一来的话，整一个程序就会以一个调色方式给启动起来。

并且停留在一个一个端口之中。好。调试起来以后呢，我们就能够哎假如上面的操作都正确以后，我们的IDA就可以进行一个正常的一个挂接了。选择debug attach to process嘿。

它就会尝试的去由我们的程序以由我们的安卓系统来进行一个链接，这里就可以看到它的通讯已经成功了。好，这里可以看一下。好，我们来选择我们的目标程序来进行挂接。好。

是这样一个F findhellolety的一个文1个APK文件，我们选择它点击确定。每这里就可以让它哎读选文相应相应的一些内容。然后下面就是这是他读取得到的一个数据。好，这里已经挂接成功了啊。

可以看到它是这样一个样子的。然好。然后。然后呢，唉这个时候我们就可以尝试去对它进行一个运行了。好跑起来。但是接着哎如果是以这种方式启动的时候，像在跑起来，整一个程序还是不会运行的这是什么意思啊？

就是我们在一个内地层已经跑起来了，但是java层还没有接上去。那么这个时候我们就需要把一个java层给启动起来，我们把monit给打开。



![](img/29c69f2d999fbe77a47d9d32620b6b39_13.png)

我们要这它它的一个java层给成功的跑控才行。找到我们的程序，启用它的80关头，然后我们用1个GDP来附加上去，让它在好成里跑起来。



![](img/29c69f2d999fbe77a47d9d32620b6b39_15.png)

嗯，附加指令的话就是这样一条，然后端口的话就是这这里打开8700好，用JDB来挂接。GTB connect。GDN。这条我太虚好烂。その方。然后是碰一个端口。



![](img/29c69f2d999fbe77a47d9d32620b6b39_17.png)

八四零零比七。好，可以可以看到这个是和调试器一样，只是用GDB来挂机上去而已。对吧然后下面就如果喜欢的话，可以用GDP的指令来输入。好。对，这里因它的一个java层已经已经跑起来了。

那么整一个程序也就相应的运行起来。那么这里它的一个第步就会加载我们的一个hello一个的一个SO文件。然后这里它就说整一个程序它打算加载这样一个文件。

他问我们这一个静态加载了一个SO文件是不是和它的原始文件是一样的。好，因为这里是一样的，就选择sam，它就会对应相应的文件进行一个分析，然后进行一个移动。好，这样一来唉。

可以可以看一下它的一个log消息。它在这样一个位置加载了我们的1个SO文件。并且它已经相应的对我网络文件进行一个重定位了。好，这样一来分析就已经完成了，可以尝试着去触发一下条件。

或者说来看一下我文们的一个断点是不是成功的下下来了。我们点击。咩。点击按钮，然后呢触发一个hello的一个指令，就会发现它已经成功了，哎，调停下来了。那么这个时候就能够正常的哎进行一个调试。

先来说一下它的一些呃操作方法，还有指令，还有端口之类的。啊，首先它它的一些快捷键的话和OD是类似的。呃，和我们的1个IDA也是类似的。首先F2是下一个断点。

那切是单幕步入F8单幕步或S9是进行S4是运行到鼠标的一个位置。我们来看一下。好，在这里比如说我想用这这样一行II2就可以看到这里变成一个红色，证明它已经写好一个断点了，可以切切切换到一个断点的窗口中。

在这个b list里面可以看到它断点已经写在这这里了。我们再在这里再按一F2就能够取消一个断点。断断点是什么意思啊？这个大程序运行到这样一个位置的时候，就可自动的给停下来。

让我们的调试器去看一下它的一个呃消息。比如说这里我们按1个F9，然后让它运行，哎，就会发现程序它又停在这样一个位置，因为它触触发了我们的一个断点。这就是一个断点的意思。我们这里再按EC次F9。

因为它下面没有没有断点了，整一个程序就直接跑起来了。我们再来点击一下，哎，让它停天那个停留到这样一个位置，看一下这里的一个以蓝色高亮的一个这样一个一行了，就表明我们当前已经停在这样一个位置了。好。

接着是1个F7A，点过去7这样的哈，一步一步往下走。F8也是一个单幕步过的一个样子。哎，单幕步入和单幕步过有什么区别啊？就区别就在于对某一些函数进行调用的时候，比如说它调用到这样一个函数。

我们这里如果按F8的话，就直接到下一下一句了。如果是按F7的话，就会跟踪进去，跟到这样一个函数进去了。我们来试一下，按F7哎再这一行按F7。就可以他发现它整一个进位到这样一个函数里面。如果是爱爱花的话。

还这里按F4，我们想用选用这样一行按F4就可运行到我们鼠标所指的这样一个一行中。MF4。好，接着这里按F8，我们按FF8A会发现它直接步过整一个函数啊，但也不会再进入到里面去了。

这一个就是一个单误不过的作用。那它的一个原理啊不它的一个意思的话和IDEA调试也是一样的。哎，还是这样一个样子。呃不我们再来一遍。好。这是ISIDA的几个快捷键。还有另外就是它的一些窗口，它的一些信息。

我们呃要给大家来慢慢慢慢的介绍一下，不然的话在后面的话会以呃看不懂的。首先是一个反变应窗口。反变义窗口也就是这样一个窗口了，可以看可以发现呢，这里反变页窗口的话，会有一个它当前的一个地址的一个说明。

比如说它当前在内存中在这样一个位置的时候，有这几个指令，哎，它是这样一一个意思。然后对应的这样一个位置的这一个比如说对应的这样一个75792104的这样一个位置，它有一条push的指令。

然后它这样一个位置有一个s的指令。然后在下一条会有一个ad的指令。这里它就一个一条一条指令下来，让它让你们来看清楚来，我们这一行有有哪一个方法可以让我们去进行一个调试。这是一个他指令的一个意思。

这是它的一个反面义窗口，可以让我们看到它的一个指定的位置的。那然后下面就是对应着指定的一些呃一些补充的一个说明了。好，这是一个反面窗口。然后在它的隔壁呃，隔壁的话可以看到一个d，这里就是一个寄存器窗口。

那么这里的寄存器唉，和我们的一个在一个呃smarly调试的时候，也是类似smar账户是有一那些一个V0V1，然后P1P2P3P4，然后V101VV314嘛。那些是寄存器。那么到这里为止。

这些阿021R2324也是继存器只是只是他们与V0V1不一样的是，它们是一规定的，必定是一是1个32位的一个继存器，他们只能存放一个32位的一个信息。啊，比如说代理阿0就代表了这样一个信息。

然后R一是1个165000069，然后R2是这样子，它是必须的1个32位信息。然后整整一个程序的话，就是利用这样一些计存器的一个数据的操作来完成咱整个AIM的一个内容的。我们来看一下，哎，这是第进窗口。

然后前面有1个20到1这样1个R8，还有说22的。然后下面会有一些特殊的一个计存器吧，前面的20到前前面的几个HG计算器的话，是作为一个立即计立即数计算器来使用的。它们用于一个参数传递，还有说一个呃。

一个数的一个运算。好，这个后面可能会讲到先忽略吧。呃这里就请大家记住，他们是立即做机机器选计。然后看下面可以看到有几个比较特殊的一点的一个计算器了。首先是1个SP计算器。

SP计算器的话就是一个对账计行器了，它是表明我们当前的一个对账位置。哎，对账又是什么东西啊，我们往下看对账这里的一个W知识存放。我们的的一个对账窗口，然后里面就是我们的我们的一个对账内容了。

对账意思的话就是哎比如说我们呃对账的话，可以让我们来存放一些临时的一些信息。比如说我们想要保存一些数据，就可以把这些数给压翻到对账里面去。那么这一然后对应的哎这对账就能够存放一些呃信息了。

比如说我们在这里看一下，在这里看一下，哎，这里就会存放了某一些函数的一些信息。这个就是对待窗口。好，还有另外一个就是LR窗口，那A是1个LR计存器。

那么LR的话就是一个标明了它的一个返回位置的一个继存的一个位置了。我们跟过去看一下，它它意思就是说当我们在一个函数给执行完以后呢，就会返回到这样一个位置了。好，这是1个LR继存器。然后PC支计算器的话。

就相当于用我们叉86折的1个IP计算器了，表明当前程序的执行的一个位置和这里是一个对应的。这里这里的PC直到这样一个位置上的话，就表明我我们的程序当前正停在这样一个位置上进行一个执行了。

这是表明当前的一个这前位置的一个计算器的。然后后面可以看到一个是一个标志计算器，一个一个标志位的一个呃一个存放。比如说一个exZF标标志位，然后一个CF标志位，这一些标志位的话就是控制一个程序。

它是是否跳转的一个呃。一个位置。然后在这里最左下角会看到有一个has view，这就是一个内存的一个窗口，就是让我们来快速的去查看某一个位置的时候的一个值的一个功能。

比如说上面唉我们的IP是这样一个样子的。我们来看一下它对应的一个内存的一个数据是怎么怎么一回事。哎，就会看到。哎，比如说它这样一个poush指令，就对应着8820B5的一个位置。

我们也可以在二镜头里面来看一下它的一个。对，看到80B5，然后是1个B4B0。好，这是一对应的。好，0AS这样的这就是一个查看内存的一个haVT吸尘器。我们按居的话可以进行一个快速的一个跳转。

装成address。好，那么这里这里我们在调试的时候，一般就是看着这样四个窗口来进行一个呃调试，还有一个修改。我们小气管利用的话都是主要在这四个窗口中进行一个执行的。然后我们调试的时候。

要注意一个荧光链路耳次耳定包包，来来及时的去看一下它们的某一些计存器的变化，还有它那一个内存的变化，甚至是它的一个对账的一个变化也要注意得到。好，那么这里我们就来对这几句来看一下。哎。

这几句这几句过后会对我们的整一个程序，或者说它的一个计算机就对上对你的内存数据会产生什么样的影响。我们来进行一个调试，来看一下这些AI代码的一个作用。好，我们这里一步一步来来注意呃。

这个时候请注意来观察一下这样一个呃计算器窗口啊，还有一个还有一个这个呃SP窗口，一个对单窗口的一个变化。好，我们先调整一下。好，看一下第一句指令是一个push的指令。

然后它它后面的一个参数是push7LR，就是这两个计存器了。我们来看一下这样一个这样一条指令过去以后会产生什么样什么样的一个变化。按哎8。诶。注意一下注意一下这里的一个蓝色哎。

计算计阵不是一个变成一个颜色啊，这里就分成了两两种颜色，一种是黑色，然后一种是蓝色。那么蓝色就表明了它当前的这样一个位置，而它上一句发生了中的一个改变，黑色的都是没变，蓝色都多表明它的改变了。

首先是1个PC计算器从上一次从上从前面的1个04变成06了。这里面程序的一个运行位置发生了中的一个改变，它目前是在样1个06的位置上面去了。接着呃接着是1个SPP就是我们我们的堆栈。

经过了这样一个p指令以后，我们的对站给发生了变化。呃，它前面的话是呃是应该是下面的这样一个位置，然后它变成了这样一个样子。好，我们来看一下，它前面是40的，然后变成了38。哎，证明这样一个对象它增加了。

那么它增加的内容是什么呢？哎，它里它里面就增加了这两个这两个数据。这两个数据是对应的什么呀？我们来来慢慢的看，首先是1个414851D0。实际上呢它就是我们我们的1个LR值。

证明它把我们LR值给放到对栈里面去了。然后上面的是1个C197，然后2D64，唉，定的就是我们R7的一个值了。好，证明这样一个破ush的值量呢，就是把我们的计算器的值给放到堆栈里面去。

然后相应的就把我们对栈的一个呃一个站给站点给加上去。这里它推的推送了两个计存器到我们对栈上去。RG还有LR给推送上去，你把LR给放到这里，然后把1个RRT给放到这里了。那他推送的时候也是这样一个类似。

你把后面的一个对积，把后面的计算器给放到底下，然后把一个前面的计算器给放到上面中去。那么在这里就给就请大家记入这样一个push的一个作用了。哎，是呃push也就是一个放入对账的一个操作。

这个是我们的一个s的一个词令了。s是什么意思啊？好，我们继续的来一步一步的看我们按哎8让它来步过。好，呃，注意一下计存器的一个变化，黑色代表没变，然后一个蓝色就代表与上面发生了改变。

然后当前对这站点是在这样1个338的的一个位置。系八。哎，会发现哎上面的什么都没变，皮写变了。好，皮些被西放到下一行。那么下面的话就不再提了。接着是一个对象给改变了。对象它本来是338。

现现在非成328了。哎，就是这这里。好，其实这样一个s命令的话，就是对应着一个英文中的一个减。就是我们把这样1个SP的值叠进去时，这里的意思就是一个哎这里有一个s就是就是1个SP。

SP就等于SP减去0叉10的意思。所以这一个指令率是等于这样一个预算好，SP等于SP减0310。好，它就是变成我们的个338，哎，本来338，然后变成了1个328。呃。

这里可以看到它对账的话和我们平时说的是逆着来的，它的占比的值会比较少。然后占比的话会会比较多，这是非常正常的啊，这个非常正常的，和一个英tel尔叉86的是一样的。好，这是什么意思啊？

这就是为这句意思就是为我们对这给初始化了这么这么大的一个空间，来让它来存放我们的一个临时的变量。好，那么在下一步是一个艾哎APP。那么想大家都知道了啊。AT和我们的。哎艾就是我们的一个加。

通过它的操作是不是一个加的意思呢？我们来注意一下，哎和上面的一句是类似，只是它的一个计算器发生了改变。看一下它物要是1个R7SP还有0。我们来关注一下R7SP2个计算器的一个变化。

SP是对称计算器还有R7是这样1个II8唉，可以发现这里的R7和我们的SP相等了，这就是什么意思啊，这是一个R7。就等于SP加上0。那么再变一下，就是就是变成了1个R7等于SP了。好。

那么这样一它一就把我们的1个RRT给去加SP的当前的一个位置。好，那么下面是为了什么作种？它就是为了这样一个后面的1个STR语句给一个作为一个。呃，准备了I加哎又是用什么样用什么样的操作啊。

我们来关注一下这几个呃财数吧。首先是一个阿里咨询器，一个阿7自选器。然后后面有个VR4。好，我们来看一下这里这里这一句过后，这里的一个。你这里这一句过后，我们的一个计算器窗口会发生什么变化？好。

当前R7是指向于这里的。好爱八卦。哎，发现这一个数据发生了改变了。然后这个数据就变成我们的1个L0的一个值。哎，这一个。

这个这一条指令的意思就是把我们220的值给存放到这样一个地址所指向的一个内容中去的。锁指向按就去了。我来看一下这条指定就是这样一个意思，1个L7。加上一个呃0叉10加VA24的。靠，这V要4的一个位置。

就可以等于我们220的值。然后来看一下VR4就等于一个-4，就是1个0310减4的一个呃一个位置。也就是一个C的一个偏移了。那么就这也就是28加C等于34，这个乘要这样一个位置了。

那么这里对应的就是一个。对的这是一个呃R7，加上。0310。加上V压8的一个位置。会等于我们L一了啊，是不是这样一个样子啊？好，VR8是1个-8，也就是计算过来的话，就是一个。8P移为8的一个位置。

也就是3个加8对应着这里的一个位置，应该会变成二页值。我们来关注一下是不是这样一个样子啊。LL一是这样一个。而发布过哎会发现是的确是这样一个样子，是已经发生有个改变。哎。

实际上就是前期非常呃就是每一条函数前面都会有的一个啊，对于对于我们的某一些计算器进行一个保存的一个操作，或者说这里是也是非常通用的，是对某一个数，或者说某一个结构体进行一个取值的一个操作的。好。

那么相下面相应的就是把一个L2给取出给放进去了。好不这样一个位置重去。好，直接把R2放进去了。接着在下面嗯。就这用用到另外一条不同的指令，这一条指令是1个LDR指令。LDR什么东西呢？

就是一个指令加载和上面的STR是对应的一个漏的一个指令。这里就是把这样一个位置的指向内容给加载到R0上去了。好，V10加VR4就是这在这里了。把它加一打到放你上去。好，这里是41400。

这是把这个值给赋予阿0。不过。哎，阿里里边是正确是正常的。那么下面也是一个加载的指令。那么加载的地址呢是这样一个地方。好，我们步过去。17B4也就是这样一个地址了。

他就把这样的一个壁址里面的内容给这样一个值给加到R3上去。然后下面的话就是对我们的一个呃进行一个从定位，给定位到这样一个函数中去了。好，慢慢步过mo指令的话，就代表一个辅子指令了，把R3放到R一里面去。

然后下面慢慢走好，看一下它最后的一个调入这样一个创建一个自助创的函数，然后创建的结果就到R0里面。然后把这样一个结果哎给返回进去啊。然后最后就是和上面pos对应的一个po指令。

就是把我们刚才的整1个R7，还有P，然后有1个L2值给赋予到1个PC上去。这样一来就完成了整个函数的一个还原，还有它的一个。返回好，这里一一步过就反回到我们对应的一个内容中去了。

那么这里就是刚才那一条呃AI函数所执行过的一个内容，呃，可以看到的框里面会有一个非常多的1个AIM指令，对吧？然后这些指令可能是我们不太懂的，哎，不太懂。那这里我说了。但说完还是不懂，对不对？

也不呃也呃那当刚开始学刚开始学的时候是不可能那么快就懂每条指令是什么怎样执行，然后它有什么效果不是不太可能了。那么这个时候的话就需要大家去多多一点来做笔记了。呃，好，这个好自信还不用纳笔头，对不对？

这里我们就需要多来进行一个呃。做一下笔记。比如说哎这里我已经给大家写好了这样一个两条指令的作用，一个必要作用，还有一个必要的作用。那么下面如果你们大家啊自己可以找一些软件来对你们的一些指令。

根据自自己的理解来写一下他们的一个注释。因为这里这里的这两条指令的话，可是我自己去进行一个修改了。好，比如说B就相当于一个跳转啊，那么这里我们是1个B2指令，就是对应成这样一个东西。好，像们好。

那BL我D注册为这样一个样子，是设试调用值程序，或者直接修改PC的值，然后相当于差不多6的po令。好，看一下看一下，还下面眼睛要注射。好，然后。你们呢就呃建议你们就根据你们自己对一个呃函数的一个理解。

对函数的理解，然后对他进行一个相应的呃一些呃一些参数，一些语句的一个进行一个注释。然后然后后面的话我就呃不太可能每一条语句，每一条语语句的一样这样来说下去。如果这样说下去的话，课程就会变得很长。

因那多方法都不能消了。好，比如说这里的BL这里的BL我已经定位到3一条上去了。我们来看一下这个BL操作，这里是我们注释了一个呃代码，直接修改PC2值，并把下一条指令的地址妨碍到RLR继成器中。好。

那么这里我们就来关注一下1个PC还有LR。首先它是会让后面的这样一个呃指针指向的位置，给直接赋予到PC上去。也就是直接修改了我们一个跳转的一个位置。好，比如说这样一个。呃。

我们双击就我们双击就能够跳转到对应的地方，按ES就能够返回到上一级的一个目录。好，比如说哎这里后面的必要这样一个值了，然后这样一个值，这必要的值就可以直接把这样一个数据给修改到P上去。

接着就会把下一句返回地址，也就是这样一个地址给放到LRLR中去。这是我写的一个内容。好，看是不是我们F7。好，关注一下PCLR唉发生了改变。首先是PC等于这样一个值LLR是等于在这里的返馈地址。哎。

那么这一个就是我注释上所写的一个地址。然后下面的话，你们自己也要在下面多点啊去啊加一下其他的一些指令，然后给扩充一下自己的一个。熟悉的一个命令。好，比如说这里有一个mo指令。好，我们找到幕指令。

然后对它进行一个修改。然后是后面跟的是两个神器。啊，这里是目标计算机。然后是原计算器。那就按照自己的理解来对这样一个啊指令来进行一个注释就好。然后目标计存器。等于原计成机。一个复值操作。好，点击确定。

启用保存。确定哎，看到的已经添加上去了。好，这样一来哎，如果遇到不懂的一个指令，就能够从网上查，然后查到它的值以后，然后理解完以后，如果实在不懂，可以去调试来看一下它的一个作用。

然后要对一些呃知道知道一个结果来进行一个呃笔记来记录一下。然后后面再遇到的时候就可以快速进行一个查找。那么这样一来哎，就能够快速的去学会这样一个押N指令。那这是我建议的一种比较好的一个学习方法。

如果下面已经有基础的，或者说有其他更加好的方法，那大家比较那大家就更加好了。那么下面的话请大家去呃熟悉一下其他的额外的一些指令。比如说在你的love的话就是空操作，就什么都不什么都不做。呃。

空操作意思什么都不会做，也不会改变一个自算器的值。好，那么这里就是我要啊这节课要讲的一个呃内容。那么下面嗯也给大家来来弄了几份资料来。

让大家来看一下我们的一个呃ERM的一个汇编的一个每条语句的一个功能呢，呃有不懂的也可以从这里去查。不过呃这个查起来的话呃，非常的麻烦。对上矛盾嗯。这有一些指令的一个讲解，呃。

一般的话还是建议大家去去查百度比较好。对于每一条指令直接查百度。啊，那这样的话嗯。那的，从这里查也是可以的。不过他自己的说明比较比较官方，呃，个人来说可能没有这么容易看懂。

还是自还是根据根据自己自己的理解来为于那些指令进行一个注释比较好。好，那么这里就是这一节课的一个内容啊。作业的话就是请大家去熟悉一下这样一个调试。就是样这样子调试，并且把自己的啊不同的指令。

不同的1个AI指令给扩充到这样一个软件里面去。

![](img/29c69f2d999fbe77a47d9d32620b6b39_19.png)

🎼好，那么这一节课的内容就到此为止了。好，谢谢大家。🎼う。

![](img/29c69f2d999fbe77a47d9d32620b6b39_21.png)

🎼Yeah。

![](img/29c69f2d999fbe77a47d9d32620b6b39_23.png)

![](img/29c69f2d999fbe77a47d9d32620b6b39_24.png)