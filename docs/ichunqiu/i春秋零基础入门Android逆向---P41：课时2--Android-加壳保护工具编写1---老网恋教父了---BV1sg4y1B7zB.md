# i春秋零基础入门Android逆向 - P41：课时2  Android 加壳保护工具编写1 - 老网恋教父了 - BV1sg4y1B7zB

![](img/01c8fe5c1044a006165dce44b78ae45a_0.png)

🎼。

![](img/01c8fe5c1044a006165dce44b78ae45a_2.png)

嗯，好的，呃，大家好，我是蒋帅吧。那么今天的话来讲安卓加可保护工具的第一课。这节课的话就会讲一个加尔墙上的一个加可解可的一个代码。那么这个这一类代码的话从网上能够找到非常多。

所以具体的一个一步一步怎么写，我就跟大家慢慢的说了。那么在这里主要是跟大家大致解释一下，它到底是用到哪一些方法，哪一些函数，还要进行哪一些操作，来跟大家来理清整一个加可解可的一个思路。



![](img/01c8fe5c1044a006165dce44b78ae45a_4.png)

好，我在这里先打开一个可程序，在这里。

![](img/01c8fe5c1044a006165dce44b78ae45a_6.png)

好我们先在看一下我们在一个java城上的一个外壳，它到底是怎么跑跑起来的。嗯，好，这里切一下版本这里。



![](img/01c8fe5c1044a006165dce44b78ae45a_8.png)

等一下嗯。然后我切到加层。

![](img/01c8fe5c1044a006165dce44b78ae45a_10.png)

好，那这里的话就是一个java层的这一个代码。那么就是每一个java层上加减密中所用到的一些函数或者说一些东西。那待会呃代码的话呃这个加密的话会给大家在这里就跟大家来简单的来说一下。

那注一下这些代码从网上可以找到。那么里面呢相对的一些原理性的东西的话，在上一节课就已经已经从一个原码上来进行了一个分析了。我们现在看一下整个安卓的一个代码说要加可时候，它的一个入口点。

正如正如我们前面都已经说过了，每一个安卓程序加固可的安卓程序我们要分析的时候都是从这样一个入口点来开始进行一个分析了。因为每个加固的一个入口点。

它加固后的一个APK的一个入口点都肯定是在这样一个plication标签中的一个安卓点链中来指出的。那么通过这样子的这么一块的话，就是一个AP程序的一个入口点。我跟过去来看一下。



![](img/01c8fe5c1044a006165dce44b78ae45a_12.png)

那么这个就是一个st application，那么这个把代码化就可以完成呃整一个dash文件的一个释放，还有加载界面，然后给替换为替换到原始的一个classword中去的。

那么这一个就是它的一个实现的一部分。我们来看一下呃，这里的话我主要重写了两个函数。嗯，第一个是attach is contest那么然后另外一个就是一个enre的函数。

并且呃大家多可的话都是通过重写这两个函数来完成一个结合了。现在说一下这两个函数，这两个函数都会在程序执行的时候被自动调用到。但是它们是有一个先后关系的。

首先这然是加一个attach base content，它肯定是第一个被调用的调用完这样一个以后呢，才会调用到这样一个onc的函数。如果不相信的话，可以尝试打一个log文件啊。

打一个log消息来看一下它的一个代码。如果不相信的话，可以在这两个函数优果点来把一个log信息来看一下它这两个到底是哪一个设别优先。因因为它们有了这两个一个加载顺序的一个关系。

所以非常多可都会选择把一个解密，把一个可的一个原始des文件释放放到这一块中进行。然后这一块就来完成一个application，一个原始application一个释放。那么在这里的话。

我就遵循着啊这原始这么一种操作，把一个des文件简面放到这里，把一个原始application的一个替换放到这一块地方。啊，那么那么记住这两个最基本信息以后，就来开始进行一个分析吧。好。

那么打开我们的第一个函数，就是在一个application contest里面的话，就可以开始去完成一个dash文件数据一个释放之后。

那么首先我会把一个程序原始的ds文件数据给通过某种程某一种方法进行一个加密，然后放到在一个access目录中去。啊，那么记住一下哦呃40目录中的这样一个文件。

再样一个A叫有inquireed的一个文件的话，这就是一个原始的一个。这是一个原始的一个application数据，它是经过我们的一个加密的。啊，这个是原始des文件数据。那么下面的操作。

下面这一块操作就是对这一个文件完成一个解密，然后进行加载。那么由于在教层上不能够完成一个呃内存中的操作，所以在这里的话是会需要用到。首先会是会需要对这样一个文件进行一个解密，然后释放到一个存储中去了。

好，那么下面这一块就是进行一些解密的一个操作了。首先第一步的话就会从这一个目录中给释放出这个dess的一个文件数据。好，上面我们就调一个contest的一个数据。比如说调一个ge的数据的话。

就能够在d的一个程序目录下创建出这一个目录中去。哎，那么创建到这个目录，这是一个临时目录，可以让我去保存我需要的一些数据。那么下面那么下面的话就会对我的一个资源文件进行一个释放。好。

那么这里可以看到一个f manager里面的话，这就调用到一些方法来释放access manager中的一个inqui的一个代码来把这一个数据给释放到这个地方来释放到这个目录中去。

就这里catch pass，然后放到这这个文件中去，然后它里面跨会调用到这样一个方法，那么它会调到这样一个第一面的方法来完成一个代码的一个解密。那么由于在这里我并没有完成，我并没有做任何的加密操作。

所以在这里的话直接就是把一个解密后面一个bu数据，你直接返回，我们跟过去看一下这样一个资源释放的一个函数，它怎么搜了。然后首先就是获取到一个acs manager卷中的一个数据。

就是访问它当当中的一个资源，然后进行一个打开。接着就以but训进的一个方式，把这样一个manage中的一个数据全部给读取到这么一个序中去。嗯，读取完以后呢。

就以一个反射的方式来调我们的刚才传进去的一个解验方法来对我们的一个。一个数据来进行一个解密，解密完以后就把它的一个文一个内容，它的一个数据利用一个fi out据呢也写到这个目录中去。好。

那么这里就是一个单纯的一个代码释放，它里面的一些函数，还有说它参数的说明的话，可以在这个目录中哎，可以在前面的描述中给找到。好，那么这里就是完成一个资源释放，还有它的一个解密。

并且释放文件的话可以放到这个地方。哎，把它放完以后的话，我们就需要对它完成一个加载，完成一个构件的。那么在加载之前在之前的话，我们可能会需要先获取到前面的一些函数一些参数些数据。

首先第一个我们是需要获取到一个cur activity的一个主线上的一个数据。那么在这里我们就调用到这样一个类的一个方法，就调到这个方法来进行一个数据获取。

那么这么一个类的话也是一样在一个安卓规末中的一个baationactivity识别中可以找到。

![](img/01c8fe5c1044a006165dce44b78ae45a_14.png)

🎼在这里的话，我们可以在这里找到这个方法。那这些安卓代码的话啊，这些反射电话代码都可以在这么一个代码中进行找到了。呃，是在在这里的话，希望大家可以尝试去找一下。好，我们找一下。



![](img/01c8fe5c1044a006165dce44b78ae45a_16.png)

这其是月。嗯，好，那在这里大家可以找一下，通过调到这个方法的话，就能够获取到啊这个结构体中。那么获取到啊这样一个结构体以后，那么下面还有一些其他的数据，比如说一个package lens。

甚至说是它的一个当前所已经加载了package的一个list史的一个数据。我们调用这样一个结构中的这个M package的一个，我们获取到这样一个结构。



![](img/01c8fe5c1044a006165dce44b78ae45a_18.png)

好，找到这样一个节构，在这里在这里这。好掌到这个结构，那么里面到底存放了一些什么样的一个数据呢？pack唉这里的话就是一个mag数据。

这是我们当才已经loaded的一个APK的文件中的一个所有package数据，这是一个对于loaded的APPK的一个引用。

这一个loadAPK我相信大家前面已经有讲解过了在loadAPK就相当于一个 cookie中的所已经被加载了一个文件的一个数据类似的一个样子。那么这是一个来是对应的多个APK文件。

然后里面就是一个ro的引用，它表明了当前所有已经加载过的一个呃package一个loadedAPK的数据。那么通过在这里我们获取到我们这个程序本身的一个package name，对应着这样的一个弱引用。

也就是一个loaded的ABK的一个结构体。在这里我们这里获取过的话就是这么一个object。

![](img/01c8fe5c1044a006165dce44b78ae45a_20.png)

这是一个弱引用嘛，若引用映射的话就是这么一个类型的一个东西。我们通过啊将从这里入手来获取到我们对应的一个loadedAPK数据。

那么在下面我们就要对这个loaded的AK中的一个class load来进行一个替换的。那么替换完成的话。就能够完成我们的1个DX文件的一个加载了。那么可以看到。

下面我们就开始来创建一个dsclass load了。哎，在这里的话就直接调一个class load，把我们刚才解密哎示范完的示范完的一个des文件数据，直接再到内存中去，然后优化到了1个ODX放到这里。

依赖于个APKLB文件放到这里。然后下面的class load的话，我们就利用到它这里的一个弱引用的一个呃loadedAPK中的一个数据。

我们通过获取这一个loaded者ABK中的一个M class load的一个一个访问一个属性来获取它的一个class load数据。



![](img/01c8fe5c1044a006165dce44b78ae45a_22.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_23.png)

好Mclass load针对着这样一个域的数据啊，里面就存放着里面的有个class load的一个属性。好，通过调用完以后，就把我们在内存中的class load数据给加载过来，加载过来以后，哎。

我们就把我们当前加载过后的class load数据给直接给覆盖上去，就是把这样一个class load给覆盖啊。那这样覆盖掉了以后，那么在下面的一个java层文件。无论它进行怎么样的一个调用。

那么他都会他他都能够直接跑到这样一个class西欧的中区来进行一个方法，进行一个类的一个加载的。哎，那么这么一来啊，这个函数跑完以后，就完成了一个加密的de文件释放。

并且替换掉它原始可的一个vaation的一个数据了。那么这是教程的1个DEX文件释放，还有说它的一个替换。呃，替换完以后，那我们就需要进行一个检查，来看一下整个程序。

它本来是不是有一个原始的application数据。如果有的话，我们就需要对它进行一个重建，并且替换掉呃loaded ABK中的这里这几个数据，一个application的一个数据。那就这个数据。

我们要对它进行一个替换。我现在就这一步操作，就在一个oncre函数中来进行一个完成的。我在这里呃我自己的话就选择了把它的一个原始application数据呢保存在这一个mini中去了。假如有的话。

就一个me data方式来进行一个存储。那么在这里它就是通过读取一个me data数据来看一下里面是不是保保存着原始的一个类的application数据。好后那么m key一个数据的话。

去以这个来做一个标签。那通过这样一个获取，假如他有的话，那么下面就通过一个反射的方法来重构一个application的一个数据了。那么在这里进行重构的话，方法还是一样的。

先找到我们的科 activity spread中的一个数据，然后从里面的话就获取到那KT spread中，它保存了一个application的数据。好，等下哈。然后找到一个bon的可c的一个数据。

就是这一个band data。来把这通过这里哎获取到它的一些其他存在一个8底的数据中呢，哎获取到一个loaded的 application in的数据。然后里面的话哎。然后里面的话就把它原有的一个数据。

哎，原始加载过以后的一个obplication数据呢给清空掉。哎，给清空掉。那么另外的话还会有一个原始的一个数据的话，就放到这一块地址。这块地址的话，在这里也是有一个地方的。

这是它初始化时候的一个abplication数据。哎，我们也需要对它进行一个获取，然后一个清空。那么下面的话就是对于我们上面的这些呃呃一个ag个实0验中的一个时。

与所有与applicationation相关的一个属性，就是这些东西全都是完成一个清空的一个操作了。那么下面的话就是一个T恤，然后进行一个清空。那么下面就把它给re掉。



![](img/01c8fe5c1044a006165dce44b78ae45a_25.png)

那么把这些呃给初始初始的时候的一些数据给全都干掉，或者说赋予到他的一些信息以后呢，我们下面就开始去尝试用到他们家的一个ma application来进行一个呃进行一个application的代码的一个重写了。

那这个ma application还是在这样一个loaded的AP的一个java文件中。

![](img/01c8fe5c1044a006165dce44b78ae45a_27.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_28.png)

然后里面就调到这个函数。那我们看一下这个函数，假如它调用成功的时候，哎，就是这样这里class，然后初始化这样一个class以后的话，它里面就获取到这样个APP数据。

也就把这些我们刚才提供掉了这个中的这么一些东西的话全都给复制上去。比如说刚才我们在一个M application中，经把它原始的一个application给目掉，也就是移除了。

那么在这样在这里调到这个函数的时候，就可以把我们的这样一个通过ad方法给添加上去。另外哎还可有一个 application前面也是应该清供掉。

那么在这里也会引反射的方法给把我们调用这样一个ma application的一个方式把重新给弄上去。那么实际上这里下面做了一步操作的话。

就是这用这样一个方法加载这样一个plication然后把它以前的所有的一个APP数据呢给进行一个替换这。



![](img/01c8fe5c1044a006165dce44b78ae45a_30.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_31.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_32.png)

一个activity识别中的1个IIRT application。那这里就由以我们的1个APP来进行了提问。好，那么下面的话哎这就行一些啊便利啊替换的一些操作了。那么把这些替换操作给弄完以后的话。

那么整一个application的数据就理论上就已经替换成我们整一个可说它整个程序，它本身的1个APP数据。那么这么替换完成，哎，这个加可这个接可，就相当于完成了。那么对于这一个程序来说的话。

还可以进行一个多个小试。呃，如果是直接从这一个。那直接使用我的代码的话，理论上是可以呃在这里理论上是可以直接去进行一个动态调试的那么假如说这里解可出现失败的时候的话，那么肯定是一个资源访问词错误了。

那那么对因为这样一个已亏损的数据框，它里面使用到了一个资源文件框，都是从我这边直接生成的。

![](img/01c8fe5c1044a006165dce44b78ae45a_34.png)

那么至于嗯至于他如何操作的话呃。可以尝试去大家去试去干一下。那么假如它加格成功的话，它里面的话应该是会启动一个销 applicationplication。

也就是加载到我们的这样一个inqui中的呃这样一个文件夹中去了。这里一个magacT写数学号就放到这样一个increry中去了。



![](img/01c8fe5c1044a006165dce44b78ae45a_36.png)

好，在这里我们也可以使用JEP就看一下我在这样一个man主器体中来进行哪一些操作。

![](img/01c8fe5c1044a006165dce44b78ae45a_38.png)

，注意一下，假如大家在下面大家来测我这个程序的时候，发现加载不起来的话，是因为它里面的一些资源的ID值没有对上。那么在这里可能需要大家去手动去修改一下ID值才行。特别是修改过资源以后。

般它的一个ID值都会进行一个改变。好看一下这个c数据这里c数据的话就保存这样一个的一个文件。那么这个文件的话就是我的可中第要加载完以后哎一个另外一个入口的一个都可在这里那么这里面的话最主要操作就是改变了它里面的改改变了它里面的一个t的一个属性了。

一个是一个什么什么行文字，假如他这边这里加载失败的话就会抛出一个异常。当他看到这样一个抛出异常的时候就发现其实这个具体是找不到。那么这里就是一个可加码失败的一个地方了。



![](img/01c8fe5c1044a006165dce44b78ae45a_40.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_41.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_42.png)

那在这里的话，哎，大家可以自己去尝试进行一个调试。At base contest。好，可以进行去进行一个调试，然后把它的一些啊各方各方面的代码进行一个领先。那么这里面的话我注意一下用到了一个反思。啊。

那么注意一下的话，这里就用到了一个反射的一个类。那么这里的反射看就用到了这么这么的一点啊操作。我们跟过去看一下这样一个反射内中，那么它里面到底做了些什么操作。

现其实呢ja耳架往上它存在了一个叫反射的一个东西。这个反射的话，它给我们的一个作用的话，就是动态的去修改我们的这些程序。



![](img/01c8fe5c1044a006165dce44b78ae45a_44.png)

我们可以动态调某一些方法，或者说动断的获取它的某一些费额值，甚至是去对它废额值进行一个设置。哎，这都是可以通过这么一个反射来进行的。那么在这里我们就提供了几个反射的一个方法，一个是一个方法调用。

还有另外一个就是一个获取域的一个值，以并且是一个下的一个域的一个值里面的话可能会抛出一个异常。那么这里的话请大家进行一个检测。那它里面的一个使用使用方法的话都是非常简单的。

只是对java的一个反射进行了一个分装而已。那么在这里就是一个价层它的一个可的一个编写。当然我们既然编写完这个可。那么我们当然就是需要对它进行一个使用了。因为我们唉那么使用使用的时候。

我们就需要对一个文件来帮我们来完成这么一个价合程序的一个操作。也就是把我们我们的壳核代码再进行一个。

![](img/01c8fe5c1044a006165dce44b78ae45a_46.png)

给放到我们需要保护的APPK中去，并且把我们需要保护的1个APBK的一个文件的1个classDS文件进行一个加密保存。甚至我们还需要修改一个可的入口点。那么这里都需要我们去用另外一个程序来进行一遍工装。

那么在这里我也写了一个java层上的一个代码来进行这样每一套工装了。

![](img/01c8fe5c1044a006165dce44b78ae45a_48.png)

好，在这里现在只有加下版本号。

![](img/01c8fe5c1044a006165dce44b78ae45a_50.png)

那么就在这个地方。

![](img/01c8fe5c1044a006165dce44b78ae45a_52.png)

那么这里的话就是一个对java呈上完成的这几步操作。呃，这个函数也是比较简单的。首先它会对我们的进行一个参数进行判断，看一下它的使用方法，一个是一个输入的AP文件。第二个参数是一个输出的的1个AP文件。



![](img/01c8fe5c1044a006165dce44b78ae45a_54.png)

然后里面它就进行了这么一些调用。首先就是对输入的输出的一个值进行了判断。那么假如成功以后就会创建一个文件来调我们我们的一个方法。这里的话就调用一个虾AP two来进行一个很便异的一个操作。



![](img/01c8fe5c1044a006165dce44b78ae45a_56.png)

我们看一下，为什么这里要进行一个反变译呢？目的就是为了把原始的AP件，或者说是反对译完以后，我的APK文件呢完成一个打包的一个作用。我们看一下参数，这里的话我反变的时候传递了一个杠S的参数。

那么这个参数传递过去的话，就可以使得它并不会去反编译它原始的1个DEX文件。那么这么一个功作过以后，它DX文件的话是一个原始的一个APK数据来进原始的DEX数据来进行一个保存的。

那么下面的话就是使用到它的一个默认框架的。

![](img/01c8fe5c1044a006165dce44b78ae45a_58.png)

那么他们他这里的话，将会把它反面以以后代码存全部存放到一个pa中去。那么存放到这中去以后，我们就可以开始进行一个加密操作。



![](img/01c8fe5c1044a006165dce44b78ae45a_60.png)

首先就是把我们的一个class DX给加密，然后放到一个acs资源目录下。

![](img/01c8fe5c1044a006165dce44b78ae45a_62.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_63.png)

然后这里就是放到这样一个s中去的一个incrme下。那么这样下面就调用到这样一个copy file方法来进行一个呃代码的一个拷贝，并且里面还会调用到这样一个incquied的一个mes来进行一个加密。

啊，这里也是一样呃，这样一个incrme和我这边的一个dequi的一个mes的话是相对应的一个加密，一个简密方法。那么在这里的话都是直接进行一个访问。那么在这里大家可以自行的完成一个比较简单的加密。

或者说呃一或某一个函数，某一个值子类的。

![](img/01c8fe5c1044a006165dce44b78ae45a_65.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_66.png)

在这里可以完成一个简单的一个加密。好，那么在这里我对完成对一个classDX释放完以后的话，就把原始的class文件给删掉。那是那一步操作。那么第二步的话就是把我们可的一个smarile代码给插进去。

那么在这里的话就是直接完成一个拷贝了。把我们资源中的一个smart代码，哎全部给拷到一个目录中去，接着把一个laber比充的一个文件给拷贝过去。对于这样一个价层上它没有la。所以这一步的话给删掉。

那么接着就是一个安卓一修改man修改的话就是主要就是为了在一个plication标签中给插入我们的一个安卓点中一个ces一个文件假它本来已经有一个plication名字的话。

那么就需要把原文件属性给放到啊这么一个plication中去了把这些操作完成以后就可以对一个文件来完成这一个回编编那么这么一个码就跑完了再跟进一下看怎么修改一个文件了。由于这是一L文件。

那么我就用完成一个M解析。那么下面在下面里就能够到它获取一个plication的一属并且获取到原始数据有获取就直接进行一个添加修改。它输出到相应的一个地方。好。

那么这里就是整一个加密解密程序的一个外壳了。我们来跑一下，看一下它的一个实间效果。那么在这里的话，我已经把它的参数给设置好了。那里面就是通过把这么1个APT文件给进行加密，然后释放到相应的个目录中去。



![](img/01c8fe5c1044a006165dce44b78ae45a_68.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_69.png)

这也就是把这个文件进行加密，然后释放到这个地方。

![](img/01c8fe5c1044a006165dce44b78ae45a_71.png)

好，我们跑一下这个程序。

![](img/01c8fe5c1044a006165dce44b78ae45a_73.png)

我来看一下啊。好，首先这是一个ABK two，这是一个反面E。啊，这是一个第一步反面页操作，接着就对它的一些文件，有个access资源文件，还有说lib资源文件进行了拷贝。

然后对个安卓min face进行修改，修改完以后就行个回编页。那么就终回编页结果就输出到这么一个这么1个APK文件中去了。好，看一下在1个APK文件。



![](img/01c8fe5c1044a006165dce44b78ae45a_75.png)

我们本面看一下这1个APK文件的一个内容。哎，它里面的一个代码已经给替换成可的一个入口点了。对吧？这里是一个process of theT，然后里面的话就完成一个代码的一个D一上的一个释放。



![](img/01c8fe5c1044a006165dce44b78ae45a_77.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_78.png)

好，那么这次就是刚成一个口的入口点，然后里面。

![](img/01c8fe5c1044a006165dce44b78ae45a_80.png)

里面的一个资源目录哎一个设试文件中，哎，这已经保存了我们的一个inquireed的一个数据了。那么可以把这样1个AP文件来尝试进行一个安装。

来看一下它能不能够在我们的手机中跑来看一下这个可是不是能够正常的实行。好后我们再来看一下被插入到来被插入的在么一些smar中。



![](img/01c8fe5c1044a006165dce44b78ae45a_82.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_83.png)

那么这里就是将会被插入到可的一个smarile代码。这些smiile代码的话都是我。

![](img/01c8fe5c1044a006165dce44b78ae45a_85.png)

通过把这么一个效中的1个ABK完成一个反变译以后啊，给获取到的一个smar代码中直接拷贝过来的。注意一下，就是我们要写在一个外表程序的时候，就是把这样1个下代码进行个编译，获获取到1个APAPK文件。

然后把APK文件进行一个反编译，就能够获取到这么一些这码层对应的一个sile代码。给获取到以后哎就能够把它拷论到这样一个资源目录中来完成一个释放。



![](img/01c8fe5c1044a006165dce44b78ae45a_87.png)

好，看一下里面的操作的话，其实就是我们的一个。价层上的一个操作了，这是一个gaification。然后里面话还有一些反射呃，说一些反射啊之类的。然后里面还有其挺多地方调到这个反射的。

比如说这里哎一个presentification就调到一个反射方法。那么这些都是一些反射的一个调用，还有一些设置。那么通过这么一些调用一些设置以后的话。哎。

就完成了对于整一个对整一个啊对于整一个代码的一个修改啊，所以是一个T取，有兴趣的话，请自行去测试一下我给出来的这一个程序，然后把它的一些代码哎给变一下。



![](img/01c8fe5c1044a006165dce44b78ae45a_89.png)

![](img/01c8fe5c1044a006165dce44b78ae45a_90.png)

好，那这里就是一个代码的一个加密解密。当然这节课的话就讲的呃最主要讲的还是最简单的一个java层上的一个夹合。像这种价壳的话，它的安全性并不高，并且可以看到，由于java层它并不能操作内存原因的话。



![](img/01c8fe5c1044a006165dce44b78ae45a_92.png)

🎼所以它不能操作内存的原因，所以在这里是需要把原始的一个亏选数据在内存中完成一个释放了，这是非常大头的一点。那，鉴于这个用的也不多。对，所以在这里只是完成一个最简单的一个介绍。



![](img/01c8fe5c1044a006165dce44b78ae45a_94.png)

🎼好，那么这节课内容的话就到此为止，谢谢大家。

![](img/01c8fe5c1044a006165dce44b78ae45a_96.png)