# i春秋零基础入门Android逆向 - P20：课时4 arm 汇编代码讲解4 - 老网恋教父了 - BV1sg4y1B7zB

悟空。

![](img/8663a7a46ddeff5363f8d8fca562af20_1.png)

🎼。

![](img/8663a7a46ddeff5363f8d8fca562af20_3.png)

大家好，我是讲师艾包。那么今天啊来讲一个阿黑面大码的第四课。那么这节课的话会讲一些呃比较基础性的一个东西。嗯，首先我们的一个课件的课，还是上一节课，还是上一节课所说的一个那个腾讯的一个东西。

我们还是用IDN来打开来进行一个分析。那么先来说一下这个ar，我们在S4GSO时候所到的的那个ar代码。啊，这通常都是一个指令集。那么我们可以在分析的时候可以看到这这些R，其后它是一个前缀性的R。

然后它后缀的话还可以看到一个V5V6或者说V7之类的。这是因为这个ar它在发展了这么多年以以以来的话，就在在追初版状上不断的不断的加入一些新的一个指令集来加快它的一个运算速度。那么这些新的指令级的话。

它就包含了不同的特性。那么随着那么也同时就出现了一种另外一种形式的一个代码。那种就是一个加入这入一个chm的一个代码了。那么这两个有什么不一样呢？哎，它们的差它们的差别就在于这这一个ar的话。

它每一行都会占4个字解码，是一个复杂的一个指令集。然后串的话，它就占两个字解码，属于简单指令集。那么串它本身所能够代表的指令就比较少。那么它是能够实现的指令也同样的比较少。

但是它好处就在于它的运算速度比较快。它解码运算的速度的话，比arm的话要快很多。那，那么所以也这也是它能够存在的一个原因。那么ar和串的话，它是不能够混合着混合着使用的。

也就也就是说一个函数啊不能不能够同时的存在的arm或者是串。但是哎可以通过某一些特殊的一个指令或比如说B叉或必要枪，然后来达到在CPU在运行的时候，再通再跳转，同时同时来实现一个切换。

这也这是它呃为什么1个S文件能够同时存在这两种代码的理由啊，是因为它能够通过这一层是这几一个指令来进行一个切换呢？那么唉这样的问题就来了，我们该如何去识别一个代码。

到底是R5还是创呢ID对对这样的识别话是非常有问题的，特别是一些被混淆过的代码的话，哎，识别起来就非常糟糕了。然，嗯那么这个时候就需要我们去人工的进行一个识别了。

那么这个时候可以使用1个LT来家居来切换一个识别的一个版本。

![](img/8663a7a46ddeff5363f8d8fca562af20_5.png)

我们回到我们的exex缩文件来看一下，我们再找一下对应的一个呃例子吧，我们找找到那个验证验证的的验证的那个函数。啊，这里这里的话可以看到我们我的每一行前面都已经显示了显显示了对应的一个字检码。

比如说这一行代码它是对应的两个字检码，如果下面里面没有话，可以到这里测置出来or选 journal里面。然后这里就是设置一个OP扣的一个大小填一个试过去回撤的话就够看到一个啊字检码。

这里这里是每一行每一条指令都是有两个字检码所构成的一个创字检码。那这里可能说哎可能碰到一些比较特殊的一些。比如说这个22A看到它是一个。它是一条指令的，其实不软其实不软，它也是一个创的一个代码。

它其实它是有两条串代码所构成的，只是IDA把它识别为4条而已。嗯，这里也可以这样看啊，先按U来进行一个de，然后把下面强制的转换成一个呃IAI可以加区把它呀进行一个强制转换。这里是一个创的一个值完名。

好，这里就变成成扣32了，是一个变成是强制识别为一个R。我们把这个先变成扣的话，可以发现它第一条其其实是一个对22进行一个复直。我这边1个A9嗯的一个。代码接着它在下面一条才是真正的一个另外一组代码。

也就是一个呃一位的一个代码，把A2把22左移两位。那么他们综合起来的一个一个效果就是这个样子了。首先是一个A9，然后左右两位好，那么这样就变成2A4了。

这也是它唉最后变成一个move R2A4的一个功能。可以看到的话，好，这里一一个一个这么简简单的一个复字功能功能的话，创它要两条字解码才能完成。那么对于RM来说，它只需要一条字解码就够了。

这是直接move R什么什么就够了。嗯，当然他们能够做到做到效果哎，还是一样的。只是说R5块可它需要更加多的一个自解码，才能实现和AIM同样的一个效果而已。嗯。但是如果唉没有这么大的话，唉。

它就能够使用更少字源码来进行一个实现了。这是一个呃创的一个字检码。跳转来的时候会有一些特别，这的确是4条来的啊，那么我们来找一个ar的一个代码。啊，比如说这里这是一个导入的一个函数。

可以看到这里是一个每一条指令都是有4个字检码来构成的。那么这里我们就教它是一个ar的一个字检码。这一个ar的一个指令。那么我们也可以马上来找一下它是由什么地方给跳转过来的。

选中这个函数按键盘上的一个叉来寻找它的一个交叉引用。呃，交叉引用的块在分析中非常常见。就是看到在哪里引引引用得到这个函数，就在什么地方调到这个函数的时候，都可以可以寻找这样一个交叉引用。

我们看一下这里在这里嗯打开看一下是吧。你过去看一下，会发现这里有一个代码段哪就能得到。那么这里的代码是这里，双击跳过去交叉引用的地方，可以看到它从这条指令唉，就实现了跳到这个军链里面。

但是这个函数名叫jo门SAN。我们再在这个函数中寻找一个交叉引用。去跳过去啊，可以发现啊这里这就是一个创的一个代码，它是从创中给跳到R上去的。那么它实现跳转的这个代码，就是这一行B叉PC哎。

那么这里我么我希望大家也还记得啊，我们上一节课已经说过了。那么前几节课已经说过PC的话，它的指向总是指向这一条指令的下两个地址。在两条指令的地址。这是一个创的指令。那么它像两行地址的话。

就是在这里加上4，也就是巴西的一个地址。它就通过这样的一条指令来实现一个跳转。啊，这种跳转格式的话，在切换创的时候，切换创AI的时候非常非常的常见，是非样非常常见的一种跳转的一个方法来了。

那么希望这个可请大家记住了好，这就是直接跳转到它下两条这里。

![](img/8663a7a46ddeff5363f8d8fca562af20_7.png)

它下两条直径呢同时进行一个版本的GPU的一个版本的一个切焊。这就是一个阿，还有一个。还有一个唱的一个区别。好，我们继续往下看，我们来说一下一个代码的一个修改。

我们这里前几节节课话都只是说了一个分析的一个方法。我们还没做到一个修改。比如说我们要完成某一些爆复啊之类的，这个时候就可能呢去需要对函数进行某一些修改才行了。

那么问题就在于我们要怎么样去来给它完成一个真实的一个修改呢？

![](img/8663a7a46ddeff5363f8d8fca562af20_9.png)

哎，修改的话可以这样来做，可以这样来做。我们先来进行相应的一个设置写。我们进来恢复一下这个函数。刚才我在修改的时候把它弄弄坏了。是设置一下函数维不在这。好，修改一下IALT加P的话，看。

这它起始地址在这样，然后他只有地址，发现它错了，我们修改回来。好，回来那么这个时候这个函数就正常了。好，我们就来尝试去进行一个调色吧，嗯，调色的时候嗯还是要该下个不准不等的下到点。

那么这里也选择一个调试调出1个OP扣来看一下。呃，调试的时候方法啊，我们来还是一样调试的话我们。

![](img/8663a7a46ddeff5363f8d8fca562af20_11.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_12.png)

我们先打开一个安卓的一个客户端，在M上，然后进行1个APP需的一个。

![](img/8663a7a46ddeff5363f8d8fca562af20_14.png)

呃，AB follow，然后把那个TCP给令起来，然后在手机中打开了我们蓝牙测试的软件，直接再开DN进行一个挂接。



![](img/8663a7a46ddeff5363f8d8fca562af20_16.png)

这是我们不需要寻找的一个。做合点的时候可以直接的进行一个挂接的。好，我们找到我们对应的一个呃呃2呃对应的一个腾讯的那个东西。在在。



![](img/8663a7a46ddeff5363f8d8fca562af20_18.png)

在这里双击，然后让它挂机上去。应该到哪天下好了，我们就来直接的看就好。我们慢慢慢慢的走慢慢的走。然后啊这已经调试起来了，我们就跑起来，然后输一些密码之类的。以后我校验。好，那么菜水已经倒下来了。

I空格的话可以在框图还有代码中可以进行一个切换。我个人比较喜欢使用这样一个一个框图。那么下面也大家在大家不习惯的话，可以使用一个代码来进行一个调试。还是一样把一个呃它一个代码给设出来。

然后慢慢慢慢的跑下来了吧。首先它前面的话呃上呃上一节课已经说过，他前面的话就是寻求一个EMV的1个gat trust的一个方法来进行一个调用。那么它就会带一个这样1个23来进行个调了。

那么那这得先来注意一下，这是一个比较差23，然后23的地址是这么1个41D5，那么切过去。骑过去哎，大家可能会问了，因这也是一个tra代码。那么通过BL插切换的时候，che过去是不是也是一个tra代码？

其实不是其实不是我们来看一下这个阿尔山的一个地址，是这一种形式的。4157什么什么第5。

![](img/8663a7a46ddeff5363f8d8fca562af20_20.png)

挪着这就是它切换的一个呃依依据了。415777D5，那么可以发现的话，它的一个个位数是一个基数，也就是说它第一位啊是这种样子的。那么像这种样子，它个位是一的话。

那么切换回去就会切成一个char的一个状态了。如果是ar的话，那么它个位肯定是零，是偶数的话，这样切过去就是一个ar的一个状态。像这种形式切过去就是一个创，也就是说哎这里这里的代码是一个串维。

我们转过去。那这是切到这个这里，然后它实际起始地方应该是低次的一个位置，只是它是一个创代码。我们选择这一行来选强制的转换成个co右键。呃，我们按C按全上的C也可以进行一个快捷转换。好。

左上一个扣就能够进行一个识别了。这里选选用函数头按P来创造创建一个函数。就会发现的话这是一个非常明显的一个呃串的一个代码，然后里面还是一样，实现了某一些跳转。好，这里也把它的一个。资金码给调出来吧。

这都是一些创按的创的一个字间码。然后这里的话就是那一个电号太转了。因为B要的时候可以B要的时候，虽然这也是跳到一个偶数的地方，但是B要的时候是不会进行一个呃是不会进行一个切换的。

所以跳过去以后还是一个还是一个呃创的一个指令。那么这里希望大大家给记着，他这里虽然他跳的跳的目目的目标地方还是一个偶数，但是B要是不会进行一个直接买的一个签案的。好，这是一个on。

还有一个chub的一个代码，我们继续跑下去。我们只关心我我们就只关心我们需要的一个地方嘛，我往慢慢的跑慢慢的跑，然后跑到下面会以发现是一个啊校验的一个函数。这里已经把一个我们的一个name。

还有pass给传到R里和RE里面去了。也可以选取内存在进行跟踪。这是我刚才输入的一个名称。然后下面的话是我刚才输入的一个密钥，它里面有两个参参数分别放在了一个R0把和RE之中，我们跟进去的话。

可以发现它有一个。首先是一个ch代码，就一个H加IN求一个代码长度一个调啊，这是它求代码长度一个就串放到阿里中去了。我们跟进去看一下，这里是BR，所以跳过去的目标地址还是一个ch的一个指令。

它也哪些错了，跳过去。接着发现它会是一个PB叉PC就是从一个创给跳到一个R的的一个地方了。P叉唉同时就进行一个值，同时会以进行一个呃机C码的一个切换。这这一个时候的PC整令应该是88加4的1个V。

所以这是1个8C跳过去，其从一个创变成一个R变R变成变创变R了跳过去。好。

![](img/8663a7a46ddeff5363f8d8fca562af20_22.png)

这已经跳了。同时哎希望大家留意一下，但是这个T也也改变了。这里我们就说它是一个标志计算器。当这个T为零的时候，表明了这这时候，我们的CPU正在执行在一个安环境之下。当这里的T为一的时候。

就表明它执行在一个创维这。最近在一个创的一个呃指令之下了，如果长期血管的话会。呃，可以报出一堆错误的，也可以看到这里是不能改的，因为它的确是非常糟糕。

我们因为这里的CPU的版本的确是一个ar强前层就改的话，会不会发现它改不了。好，我们让他们跑过去。好，我们从刚才从一个R给跳到一个创的时候，这个时候我们的PC这里的T有个标志位。

一个创 arm的一个标志位，也同时给切成一了，证明这里是跑在一个chm的一个代码中，这就是这样一个T标志的一个这种。那么上面的话还有还是就是它的另外一些标志位。比如说呃一个零标志位。

还有说一个呃有呃符有符号进位标志位都是一样的。都是标标标志的相关的一些标志的一个代码。好，我们回到我们的这个函数来运行。那这是up和 child的一个切换。那么这里的话我们就说一下一个函数的一个修改。



![](img/8663a7a46ddeff5363f8d8fca562af20_24.png)

慢慢慢慢慢的跑下去嘛，慢慢慢的跑下去。那下面的代码我们都不怎么关系了，直接跳到函数结尾。好，看到它返回1个2040，说它是一个结束，我们修改它直直接直接让它返回一。

那么这个时候哎他就已应该成一个成功的一个样子了。好。那么这样跑下去，那么这里函数已经成功了，那么就会缺少。那么我们怕爆破的时候，我们要对这个函数实现一个爆破的时候修改方法也是一样的。

直接把20给改成一个呃成功也就好了。啊，那么我们只要改了哎改的方法也是一样的还是一样的。好，嗯，现在看一下好，下来看一下。我们改个方法的话。我们来看一下一个黑子，在LR这里是一个。呃。

是一个M的一个指令。嗯，跳过去的时候要时间相关的一个相应的一个值，自己买一个替换吧。如果大家大家不知道它返回了。那么怎们这来实现一个呃报复，我们对这这些字检码进行一个修改。那么我们要怎么去进行修改呢？

我们想要修改，得事先知道它的对应的一个字检码。比如说这行指令，我们知道这行指令对应的字检码是这个样子。那么如果是其他的一些指令，它是对应对应着怎么样字检码。哎。

那么这个时候修改的时候就另外一些软件就然派上用场了。比如说哎这个呃无名小写的这个软件的话，哎我们把SO拖进去进行一个反变。我们再来一遍吧。呃这个软件刚出的话，bug是挺多的。嗯，大家多试几遍吧。好。

进行一个反面译。选择我们选个函数双击进过去，那么这里就就可以出现它对应的一个会员代码，还有它的机器码。我们双击哎就能够完成对应的一个修改了。我们直接目回个阿0。20，然后给他一个0。然后后面看。

后面的话接下是一些B叉LR进行了返回。看他从一个出一个 arm，希完没创过去。那我得你就是几个。嗯，修改了一个地方。也这里也可以直接进行一个快速一个汇编。呃，效果也是一样的。

也可以在里查到对应的一个字节码。那么给你出来结果哎，是就是这一个样子了。好。就是就是这个样子了。好。好，这个是00201后7。74的一个代码。啊，那么这是呢它的一个编例的一个效果啊，可以看一下的话。

它的效果还挺不错的。有一些代码啊，或者说有一些呃编历的一个东西想要快速查询的话，可以在这里直接的去进行一个使用。那当然这当然，那么同类的软件，比如说查一个代码软件的话，呃，市面上有挺多。

那那么大家选择自己习惯的去使用就好。ほ。问你。呃，进行相应的一个修改。首先是一个呃给他返院值给返院为一。也就是把放到22浏览值给放44乘1，然后直接进行一个返回B叉LR，这它跳转给LR的指针。

也就是它返返回地址的同时进行一个代码的一个斜号成状。成哪一个叉。我们查一下这两个对应的一个日间码自然码还什么？这是一个创代码，那么对应下字节码看。是这个样子的。好，那么看到是0120774。

然后这里的话哎在一个哎在一个IIDA中它选择一个ed，然后一个pch program by时可以直接修改它的一个代码。



![](img/8663a7a46ddeff5363f8d8fca562af20_26.png)

确定，那么他就对就直接进行一个返回了，直接进行一个返回了。那么这就是一个爆破人的方法哈，那么他反而说。这是个sh for在有些地再改错一点点。嗯我们再来一遍吧，再一遍，理论上的话呃是没问题的。

这写代码这样改，对吧？理论上没问题了，大才不知道为什么报存错误，你直接选择一个dista来忽略就好。Twice track。哦哦哦哦嗯。呃，性质错误啊，不管它。那么这是一个爆破的一个修改的一个方法。

如果我们不想爆破的话，哎，我们。我们报酬并不是一个目标，对吧？我们来说一下，哎，还有什么办法可以进行一个呃其他的一个东西吧。我下面的话，我最后来说一下一个跳转的一个指令吧。呃，那么在跳转的时候。

我们在下面自改的时候，其也会遇到一些经常需要进行一个跳转的一个修改的一个地方呢。

![](img/8663a7a46ddeff5363f8d8fca562af20_28.png)

那，这些跳转的话可以改变我们还做一个调用和所引用之类的。那么这些跳转应该怎么改，它的一些值令是怎么计算的呢？这里需然我们可以快速的去查到某一些呃某一些代码，但是跳转时候的一个代码的话。

这里还是没有没有办法来进行一个编译出来了。那么这个时候就可能需要我们去进行手动的一个计算。那么它的一个计算方法也是很简单的。它的计算公式就是这个样子，这个目标地址减去当前批次地址。

然后出一个指令指令的一个长度。

![](img/8663a7a46ddeff5363f8d8fca562af20_30.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_31.png)

那么到到这个创代码中就是这个样子了。首先啊是一个目标地址，是这里1个78的一个地址。好，这是我们的一个目标目标地址，然后减去当前PC的地址。比如说我们当前PC在这个地方，它跳转到这里。

那么这个时候BPC是B0，加上它2乘以字检码长度，它是一个串名的加4，那么就是PC的值是这个地方，我们减去PC的值。好，它变成复数不用管。然后除以一个呃字节码的的一个长度，这是2除以2。

那么这是一个FA，就是对应着这一这一个地址，这是FA。如果把它改一下，把它变成1个FB的框，哎，就会跳到这一行地址，那来试一下进行一个修改。AddD andH problem选bu，然后把I改成B。

好那么可以看到的话，它就是跳到一个这种地方了。好，这跳整地方。呃，这是一个R它一个创代码的一个跳整的一个计算。呃，到了1个AIM中也是一个鬼样子的，也是同样是一个鬼样子的。那这个时候可能大家微问可问了。

为什么在你后面还要除以一个指令长度呢？对吧？其实根本不除以也是可以的，对不对？哎，就是因为它是为了增加它的一个跳转跳转的一个范围了。因为对因为对一个创维来说，哎，它可能最大跳转。

也就一个FF这样的一个地址实在太太太小了。

![](img/8663a7a46ddeff5363f8d8fca562af20_33.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_34.png)

而且对于对于一个创或者说对于ARM来说，它每一行代码的长度度长度都是固定的。比如说创它每行代码都是固定两个字节的一个长度。那么ARM的话，它是每一行代码固定4个字节的长度。那。

那么这个时候我们如果不处以时间不除以只指定长度的话，就可以出现了一些浪费，对不对？所以说嗯那么这里的一个跳转，你可以理解为这里就跳转呢和它偏因为这么多的一个子令，哎，就可以这样理解。

呃B是这里则是一个呃。修反加一。嗯，AB。去饭。那么这里就是跳转5个字5个指令级指令长度。好，它就是从这里跳转12345。哎，它就是它意思就是跳转五五行大五行指令。那么对于一个阿姆来说。

我们找一个阿姆的一个例子来说一下。那我一址找不到啊，这也不好找，其实不好找。呃，按 arm对ar来说还怕跳转是一个样子的，就是也是一个样子的。这以找不到一个例子哎。

可可以找到一个PL race的一个方法。它的目标地址是这是这样的，是这样，目标地址是这个样子。然后他跳转的时候啊，他的地址是这个样子。那么它对应的一个跳转的跳转的长度的话是AF5FBF51。

这是一个东西。我们来看一下是不是啊。好，我们来看一下这BL位置的时候，它目标地址呃是1个5C。这时候使用使用计算方法，就是使用一个目目标地址。减去当前BC的地址。当前DPC地址的话呃。

是个DC加8的一个地址，然后减再减去8。因为它下两行对于R来说，它下两行地址就是48了，减完以后再除以它的每一条指指令的一个长度趋势。好，就是这1个FFFFFB51就是对应着这里的一个跳转。

所以大家在上面想要自行的修改一个跳转的一个目的地址的时候，也可以哎把它对应的一个低位的一个地位的一个偏离给改一下。啊，对于安来说，它单位的这几个的话，就是不是代表着跳转了一个fag的标志位了。

那么它地位的话就是它跳跳转了一个偏移。这是一个跳转的一个计算。那么下面进行一个相应的接管话，就能改到相应就能改一下相应的一个跳转的位置了。比如说的话我想这里。这里想让它从自身给跳回到自身，对吧？

从1个DC跳到DC里面，我们知道怎么改呢？嗯，要改方法还是一样的话，就是使用一个。嗯，目标地址目标地址是TTC。减去当前的1个PC地址，减这个再减8就是当前PC地址了。然后再除以一个职业长度是4。好。

首先它应该是FFF51，我们来改一下。FF1好，那这里就从一个呃。是不是这样，我们是不是改错了一点点。应该是改错一F1这是FD。好，那么这就实现了一个从自身跳转到自身的一个死循环。

然后这也是一个跳转的一个偏移的一个计算。有那当如果是跳转到继存器的时候，比如说刚才呃调试的时候，已经看到1个B叉23的那种跳转方法，跳转到自身继存器的那种。哎，那么那种就不需要这么麻烦。

直接把目标地址给赋于赋于到一个继承器之中，然后进行一个跳转就行了。

![](img/8663a7a46ddeff5363f8d8fca562af20_36.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_37.png)

哦，就是就是在一种样子，先给R3一个目标变准CBL叉L3来实现一个跳转。样过来就已经完成一个跳转了。好，那么这是一个跳转的一个修改了。那么这也是我呃在这一节课中想要补充的一些小的一个知识。嗯。

那么大家家要在下面的话，遇到一些SO文件，一一些SO程序。那么里面有一些代有一些代码的内容，哎，是我们大家不想看到的话，就可以直接的去进行一个修改，进行一个爆破。那么这也是可行的。嗯。

那么我们只要把一个比如说在我们对一个S文件进行一个修改，我们重新来一遍吧，我们对S文件进行一个修改。这里直接在IDID中IDA中进行一个修改吧。好，我们进行一个修改。好，把对应的代码该怎么改的，怎么改？

这是一个这样的一个代码，我们来直接。うんうううんうん。啊，比还是我还是比较喜欢自写的这个。嗯，这是一个创办。然后一个B叉。有2。皮。好，直接拷一下这解码。直接的完成一个程序的修改以后。

我们直接配 page program。完成完以后的话，我们想要保证我们修改。呃，这个时候我我我们并不需要使用什么一个has的一个什么hasway啊，那些ha什么东西来。来进行一个修改。

我们直接adit然后patch page program，然后那个apply page into里面就能够自动的完成一个修改了。好，那么这里唉这是设置是我们来修改过后的1个SO文件，然后。

它下面的话还是应该会有一个back out文件的。啊，这里我我就没有保存一个备案靠。我这边要保存一个备答。呃，我们使用相关的一个对比软件，也可以看一下它的一个修改。这是一款对比软线。

我们来看一下这是我完修改过后，被patch过后的1个SO文件。这是它原的1个SO文件。然后对比一下，有一个地方哎，有4个字金码不同，这就是我修改过后的一个地方了。哎。

那么在这家IDA中进行修改也是可以的。也是挺可以的。那么对比着在1个SO help中进行一个修改。那么不知道大家更加喜欢怎么改呢。对吧那么加这里就要看各位的习惯和不一样而已。不同而不一样。

那么我们这里修改过后的一个SO文件，这里就是我们所谓的一个爆括后的一个SO文件了。嗯，那么这里的话呃这里我们想要把这个修改后的软件。那么下载括和SO文件。

直接给push到一个手机软手机上的话就能够使我我们的一个修改生效了。把我们的传过来我们现在进入到一个手机的一个。



![](img/8663a7a46ddeff5363f8d8fca562af20_39.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_40.png)

目录像。然后我们来找到它存放1个SO文件的地方。那么每个每个APK它在安装的时候都会以把一个SO并拷虑到一个目录下，是这个data，然后1个APP，然后是1个LID。是这是腾讯。好，那们可以看到。

对它本来的话就有一个S文件，是它刚解压了，我们就把我们的个S文件给push到对应的位置上，就能够完成一个替改替换完成一个爆破修改了。那么当然这里直接push过去的话是没有权限的。

那么我们先推送到临临时目录像。

![](img/8663a7a46ddeff5363f8d8fca562af20_42.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_43.png)

然后TMP嗯。好，那么这个时候我们就可以进行一个拷贝了。我，拷贝直接用CP1个指令，然后把我们刚才布署过去的一个文件，一个是文件。然后烤到这个当前的一个木下。好，拷贝好，我们看到这里已经拷贝过来了。

它但刚拷过来的话，它是还没有一个实行权限的。这个时候如果运行的话，直接就可以挂了，我们先赋一个权限。嗯，还是用CHMD指令。



![](img/8663a7a46ddeff5363f8d8fca562af20_45.png)

嗯，那么这个时候就可以在我们的一个程序上看到一个效果了。嗯，我们先把原来的1个APK终止，那么这样让它再重新打开的时候，才能够加载我们修改过后的1个SO文件。那么大家这里可能看不到效果。

那么大家在下面的话可以自行的去试一下。这是一个SO的一个修改以后把它抛爬生效的话，不需要重新打包的，直接把它push到这个位置上就行了。



![](img/8663a7a46ddeff5363f8d8fca562af20_47.png)

那，那么可以看一下这个位置的话，在在在在在我们放在上应节目的话，可以看到这也就是一个软件安装的时候存放在1个SO位置。我送了这么多软软件，然后它就解压到SO在这么多地方了。



![](img/8663a7a46ddeff5363f8d8fca562af20_49.png)

![](img/8663a7a46ddeff5363f8d8fca562af20_50.png)

好，那么这一证明这个就是这一节课的一个呃要再讲的东西了。那么这节课到此为止，谢谢大家。

![](img/8663a7a46ddeff5363f8d8fca562af20_52.png)

🎼홍。

![](img/8663a7a46ddeff5363f8d8fca562af20_54.png)